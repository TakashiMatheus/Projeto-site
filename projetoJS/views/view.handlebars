<div id="job-view-container" class="container">
    <a class="back-link main-color" href="/">Volar</a>
    <div class="row textcenter">
        <div id="img-container" class="col-md-12">
            <img src="/image/company.png" alt="Company">
        </div>
        <div class="col-md-12">
            <h1 class="main-color">{{job.dataValues.title}}</h1>
            <p class="bold">Quem eles estão buscando:</p>
            <p class="job-desc">{{job.dataValues.description}}</p>
            <p><span class="bold">O salário informado é de:</span>R${{job.dataValues.salary}}</p>
            <p>
                Para trabalhar na <spa class="bold main-color">{{job.dataValues.company}}</spa> envie um E-mail para a <a class="main-color" href="mail:to {{job.dataValues.email}}">{{job.dataValues.email}}</a>
            </p>
        </div>
    </div>
</div>